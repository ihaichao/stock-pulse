# Stock Pulse — Environment Variables

# Database (MVP: SQLite, Production: PostgreSQL)
DATABASE_URL=sqlite:///./stock_pulse.db

# LLM endpoint (local HTTP service, compatible with OpenClaw)
STOCK_PULSE_LLM_ENDPOINT=http://127.0.0.1:9001/llm

# Finnhub API key (free tier, for macro calendar)
FINNHUB_API_KEY=

# App settings
APP_ENV=development
APP_PORT=9002
